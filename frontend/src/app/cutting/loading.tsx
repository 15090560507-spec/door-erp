import LoadingPanel from "@/components/workspace/LoadingPanel";

export default function Loading() {
  return <main className="workspace-page workspace-page--wide"><LoadingPanel rows={8} /></main>;
}
