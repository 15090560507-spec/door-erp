# 订单、整单 BOM 与轻量 MRP 后续工作清单

更新日期：2026-09-14
当前分支：`feat/semicircle-handles-and-quote-form-improvements`

## 当前已完成

1. 已终审图纸和对应报价可作为销售订单来源，同一订单限制为同一客户。
2. 订单确认采用幂等生成，按数量创建独立门樘生产编号和技术快照。
3. 已建立版本化整单 BOM、自动生成规则、校验警告和发布接口。
4. 门框加工结果可回写整单 BOM；门框具体下料规则按最新要求暂缓完善。
5. BOM 发布后可生成库存需求、采购需求和车间工作包。
6. 工作包支持依赖、物料齐套判断、受控跳过、批量完成和加权进度。
7. 已建立经营管理共用页面组件和当前视口居中的 Portal 弹窗。
8. 订单确认页面已拆分为列表、摘要、整页编辑、来源选择、订单行和履约状态组件。
9. 已新增 `/cutting` 整单 BOM 下料工作台，并替换导航中的旧下料入口。
10. 下料工作台已支持订单/门樘归集、状态筛选、BOM 生成、物料与数量编辑、自动保存后批量核验、版本查看及发布。
11. 已对下料页面增加窄屏单列布局，宽 BOM 表格只在明细区域内部滚动。
12. 已补订单确认到 BOM 发布的跨模块回归：重复确认、重复发布均保持幂等，服务重启后订单、门樘、BOM、物料需求和工作包状态可恢复。
13. 采购、库存和基础资料工作台已接入统一页头、真实业务指标、页签和错误提示；库存“在途”按采购单状态统计，不再手填进度或按订单重复建仓。
14. 生产管理已完成第一轮工作台改造：关键履约指标、可筛选生产阶段、订单/门樘主从详情和工作包页签统一展示，原有质检、入库、异常及变更单逻辑保持不变。
15. 效果渲染页已完成首轮切换性能治理：素材库按需加载，图纸任务请求可随页面卸载取消，裁剪器和分层效果工具改为延迟组件。
16. ESLint 已排除 `.next-stale-*` 历史构建目录；AppShell、公共弹窗、报价历史、分层渲染及本轮经营工作台的 effect 问题已修复，真实源码错误由 81 条收敛至 17 条。
17. 旧任务总览、门表单、鲁班尺、配件库及库存目录的剩余 17 条 ESLint 错误已全部修复；配件弹窗重复加载已移除，异步目录请求增加卸载保护，全项目 `eslint --quiet` 和生产构建均通过。
18. 公共 API 层已统一提取业务错误、字段校验、超时和网络异常；CAD 生成与预览失败弹窗会显示具体原因并可直接重试，BOM 工作台复用同一错误解析；Next.js 登录拦截已由弃用的 `middleware.ts` 等价迁移至 `proxy.ts`。
19. 历史图片字段迁移已修正：旧 `ref_img_b64` 在保存和读取时会先转换为 `ref_images`，清空单图字段会同步删除落盘文件；数据安全 47 项、库存基础 17 项、订单至履约核心链路 23 项测试全部通过。
20. 自动备份文件名已增加同时间戳递增后缀，解决 Windows 时钟粒度下连续快速保存可能覆盖前一份备份的问题；独立数据安全回归仍保持 47/47 通过。
21. 基础资料已新增可视化“BOM 规则模板”：规则可关联内部物料，按产品、门型和一个附加参数匹配，支持每樘/每扇数量、损耗、供应方式、优先级和启停；启用规则会追加到新生成的草稿 BOM，已发布版本不受影响。
22. “下料”已归入经营管理并位于订单确认之后，页面统一命名为“BOM与下料”，明确展示“BOM准备、核验发布、下料执行”三个阶段。
23. 已新增统一用户视角的 `Door-ERP-使用手册.md`，覆盖完整单据链、各模块操作、常见问题、腾讯云更新和暂缓功能。
24. 经营管理已切换为临时预览权限：仅销售小A显示并可进入六个经营页面，其他账号直达相关路由时返回任务总览；小A旧默认密码会在启动时一次性迁移为新密码。
25. 订单实收已与收款计划分离：支持一笔收款分配同一客户的多张 `TM` 订单、收款冲销留痕、订单实时已收/未收汇总。
26. 履约发货已改为读取 `TM` 订单实收；尾款未收齐默认禁止发货，授权放行后欠款继续保留，单樘重复收款入口已对 `TM` 订单关闭。
27. 已完成一次不中断的后端脚本巡检：44 个脚本中 39 个按默认配置通过；3 个旧生产脚本因旧模块默认停用而返回 `404`，显式开启测试开关后全部通过，确认不是现行业务回归。
28. 基础资料物料编辑已统一使用当前视口居中的 Portal 弹窗；库存总览在窄屏改为信息卡片，避免整页横向滚动，桌面端继续保留高密度表格。
29. 采购缺口、供应需求明细和库存流水已补齐响应式卡片布局；宽表只在桌面宽屏显示，供应需求同时接入公共 API 错误解析器。

## 尚未完成

### P1：经营管理四个工作台

预计 2–4 小时。

- 四个工作台均已完成第一轮统一；库存、采购、供应需求、库存流水窄屏布局和基础资料弹窗已完成。
- 后续只清理低频旧页面中的横向滚动和散落深色按钮，不再扩大经营管理功能范围。
- 经营管理目前仅销售小A可见；后续页面稳定后再扩展正式岗位权限矩阵。

验收：四个角色工作台共用同一单据链，库存和采购进度来自真实事件，不能手填百分比。

### P1：导航性能与错误治理

预计 0.5–1 小时。

- 继续观察效果渲染在真实数据量下的页面切换耗时；首轮延迟加载和请求取消已完成。
- 公共错误解析和 CAD 失败重试已完成；继续把同一解析器接入剩余旧页面。
- 源码 lint 错误已归零；后续只保留构建警告治理和真实数据量下的切换观测。

验收：主要导航切换无明显卡顿；局部接口失败不拖垮整个页面。

### P0：迁移、全链路回归与上线

预计 2–4 小时。

- 历史图片迁移、重复确认、重复发布和服务重启恢复测试已通过；上线前仍需对腾讯云数据卷做备份演练。
- 真实后端下验证订单新建、保存、修改、确认、失败重试、取消和刷新恢复。
- 验证“订单确认 -> 门樘 -> BOM -> 库存/采购 -> 工作包 -> 质检 -> 成品入库”。
- 已完成一次不中断的 44 脚本巡检。现行业务核心回归通过；旧生产模块的 3 个脚本在显式开启后通过。
- `test_cad_new_options.py` 尚有 9 条图形基线待按当前模板实图复核，集中在气窗六格、花件镜像、反面门框和尺寸标注间距；不得直接按旧断言回退现行规则。
- `test_api.py` 是依赖运行中服务和 `requests` 的旧手工联调脚本，不纳入自动回归；上线时改用真实后端联调清单验证。
- 检查 1440×900、1920×1080 和 390×844 三种尺寸。
- 最后推送 GitHub，并在腾讯云拉取分支、重建 backend/frontend 容器。

验收：历史数据不丢失，重复操作不产生重复门樘/BOM，核心回归全部通过。

## 明确暂缓

- 3D 下料预览。
- 外皮端部异形及完整上下框加工规则。
- 门框具体下料规则深化。
- 当前零件 DXF 和全部单件 DXF ZIP。
- 细粒度角色权限配置。
- BOM 版本逐行差异对比界面；后端差异接口已经保留。
- 下料工作台内的门框加工页签和门框结果可视化；旧门框工具仍可通过 `/door-cad/frame` 独立访问。

## 建议继续顺序

1. 按实际岗位优先级扩展经营管理四个工作台。
2. 集中处理导航性能与统一错误提示。
3. 最后完成历史迁移、全链路页面回归和腾讯云上线。

按当前收紧范围，剩余有效开发时间约 1–2 小时；主要是 CAD 旧基线逐图复核，以及需要用户在腾讯云执行的备份和上线验证。整单 BOM 下料、经营管理四个工作台、经营管理核心窄屏布局、BOM 规则维护、效果渲染首轮性能治理和源码错误清理均已完成。
