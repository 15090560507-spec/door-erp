# 图纸绘制页快捷出图实施计划

1. 扩展渲染 API 客户端，支持按任务 ID 查询渲染进度。
2. 在图纸绘制页增加 DXF 表单指纹缓存，预览后复用同一 Blob 下载。
3. 接通当前任务线稿、页面参考图、默认模型和快速 2K 渲染任务。
4. 在当前页面展示渲染状态、错误、结果图、下载与重新生成操作。
5. 横向排列三个主操作，删除重复下载按钮，将保存修改移到页面最下方。
6. 运行前端构建、渲染针对性检查和 CAD 基线回归。
